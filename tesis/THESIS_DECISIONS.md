# Thesis writing decisions

Running record of the choices made while writing the thesis in this folder, so that any agent (or the author) picking up a later chapter applies the same conventions and knows what earlier chapters promised, deferred, or deliberately left out. Update this file whenever a new decision is taken. Last update: 2026-09-21 (section 5.3, chapter 5 complete).

## 1. Build and check

```bash
cd tesis && latexmk -pdf -interaction=nonstopmode thesis.tex   # biber + pdflatex, all passes
grep -c "^! \|Undefined\|undefined" thesis.log                   # must be 0 (grep only AFTER latexmk has exited)
grep -c "—" chapters/*/*.tex chapters/Bibliografia.bib           # must be 0 everywhere (no em-dashes)
pdftoppm -f N -l N -r 70 -png thesis.pdf /tmp/page && <look at the PNG>   # always look at new pages
```

`thesis.log` line 299 `\end{dedication}` is a pre-existing hyperref duplicate-destination warning, not an error. `\nocite{*}` is commented out: uncited bib entries do not print.

### Whole-thesis review routine (set up 2026-09-21; trigger changed the same day)

**The review runs only when a whole chapter has been finished, or when the author asks for it** (author's decision, 2026-09-21: three cold readers of the entire thesis cost too many tokens to be spent after every section). When a chapter is finished the whole thesis is reviewed, not only the part that changed: the `thesis-review` skill (`/thesis-review`, files in `.claude/skills/thesis-review/`). It runs the mechanical checks, has three fresh-context reviewers read the entire thesis cold (flow of information, internal consistency, sceptical examiner; they never see this file or the code), then verifies every finding against the source, this file and the code, and writes `reviews/YYYY-MM-DD_review.md` in the format of `CH4_CORRECTIONS.md`. **It applies nothing**: fixes are made only on the author's command ("apply B1 B3"), after which decisions are recorded here (§5, §6) and declined findings in §9.

The trigger does not rely on memory: `scripts/review_state.py` keeps a copy of the reviewed sources in `reviews/.snapshot/`. A chapter counts as finished when every `\section` and `\subsection` heading of its file has text under it and the file has at least 300 words; the review is due when a numbered chapter (`chapters/NN_*`, title page excluded) that was unfinished in the snapshot is finished now. The Stop hook asks for it once per finished chapter, the SessionStart hook announces it after hand edits. Edits to a chapter that was already finished do not trigger anything, however large (the changed words are still printed, for information). The first version of the trigger (400 changed words, or 10 paragraphs with 50 words) was dropped. The constant `MIN_CHAPTER_WORDS` is at the top of the script; `python3 .claude/skills/thesis-review/scripts/review_state.py status` shows the current count.

## 2. Global conventions

- **Language and spelling.** English (`babel english`). Oxford style as used in chapter 2: `-ize` / `-ization` (optimization, parametrization, normalization) but otherwise British (behaviour, centre, manoeuvrability, labour).
- **No em-dashes anywhere** in thesis prose, captions or bib. Use commas, colons, a parenthesis, or a new sentence.
- **Register.** Flowing prose paragraphs; `\emph{}` on a term at first use; numbered display equations with `\label{eq:...}` referenced by `\eqref{}`; sections, tables and chapters referenced by `\autoref{}` (English names, "section 2.3", "chapter 5"). Numeric citations `\cite{key}`; several keys in one `\cite{}` when citing a group.
- **Notation standard: everything that is not a scalar is bold** (supervisor's request, 2026-09-21; applied to chapters 2, 3 and 4). Vectors are written `\vect{x}`, `\vect{\theta}` and matrices `\mat{W}`; both macros are defined in the preamble of `thesis.tex` as `\bm{#1}` (package `bm`, bold italic for Latin and Greek alike). Never write `\mathbf` or `\bm` directly, so that the style is decided in one place (e.g. `\newcommand{\mat}[1]{#1}` gives plain matrices). **The convention is applied but not stated in the thesis**: a notation paragraph at the start of chapter 2 was written and dropped the same day on the author's decision (2026-09-21); do not add it back. Plain italic: scalars; the single components of a vector or matrix (`x_j`, `W_{ij}`, `q_j`, `w_{i,z}`, `\tau_{\mathrm{act},j}`); the ABCD coefficients when they are discussed as the coefficient of one synapse; **the elements of abstract sets, i.e. the states `s` and actions `a` of the MDP in 2.1** (as in Sutton and Barto), while the concrete seven-command vector of chapter 4 is `\vect{a}`; sets (`\mathcal{S}`, populations `P`, `Q`, fronts `\mathcal{F}_i`); axis names `x`, `y`, `z`. Bold: parameter vectors (`\vect{\theta}`, critic `\vect{w}`, also as subscripts: `\pi_{\vect{\theta}}`, `V_{\vect{w}}`), RNN/LSTM states, gates and biases, CMA-ES `\vect{m}`, `\vect{x}_k`, `\vect{p}_c`, `\mat{C}`, solutions `\vect{x}` and objective vector `\vect{f}` (and the points `\vect{p}`, `\vect{q}_i` of `fig:pareto`), Hebbian `\vect{x}`, `\vect{y}`, `\vect{w}`, `\mat{W}` (but scalar `y` of the single unit), the morphology `\vect{m}` and the controller `\vect{\theta}` of chapters 2.5 and 3 (bold also separates them from the mass `m` of chapter 4), and in chapter 4 `\vect{q}`, `\vect{v}`, `\vect{c}`, `\vect{\tau}_{\mathrm{act}}`, `\vect{F}_{\mathrm{a}}`, `\vect{f}_{\mathrm{c}}`, `\vect{a}`, `\vect{a}_0`, `\vect{\omega}`, `\vect{w}_i`, `\vect{v}_{\mathrm{cm}}`, `\vect{F}`, `\vect{g}_{\perp}`, `\mat{M}`, `\mat{J}`, `\mat{R}`, `\mat{D}`, `\mat{K}_v`. Accents go outside: `\dot{\vect{v}}`, `\hat{\vect{F}}`. The commented-out PCA section of chapter 2 was not converted. Chapters 5 to 7 must be written with the macros from the start.
- **Sources of the background sections** (supervisor's request, 2026-09-21). Each of 2.1 to 2.4 names its main source in its opening block: 2.1 Sutton and Barto; 2.2 Goodfellow et al. (worded as "a textbook treatment is given by", NOT "follows", because the section was written from course slides: the author may replace it with the source actually used); 2.3 Eiben and Smith, then Hansen's tutorial (CMA-ES) and Deb's book (multi-objective); 2.4 Hebb + Gerstner and Kistler (article and book). Every figure is mentioned in the body text with `\autoref` (checked 2026-09-21: all are; only the two subfigure labels of `fig:drone-actuators` are unreferenced, by design).
- **Formulas and figures only when they add insight** (author's rule, 2026-09-17). A formula that merely restates prose is left out.
- **Cross-chapter pointers: keep to a minimum** (author's rule). Chapter 3 has none. Subsection 2.5.3 has two (to chapters 3 and 5), approved.
- **Bibliography.** `chapters/Bibliografia.bib`, biblatex + biber, `style=numeric, sorting=nyt`. Keep the existing key style `authorYEARkeyword` (e.g. `gupta2021embodied`). Proceedings versions preferred over preprints; arXiv only when nothing else exists (`@misc` with `howpublished = {\url{...}}`).
- **Glossary.** `chapters/XX_Glossario/XX_Glossario.tex` is a manual `itemize` of acronyms in order of first appearance. Add every new acronym there.
- **Figures.** Go in `images/<chapter folder>/` (folders now match chapter names, see §7). Tikz figures use the preamble's shared styles (see the Hebbian locality figure in chapter 2). Figure titles: write "p-value" not "p", "p-value < 0.001" below one per mille, "progress" not "P", bare figure-level titles with no parenthetical details.
- **Reference drone naming.** Plots call it "Bixler (generalist controller)" (`pareto_fronts.BIXLER_LABEL`); chapter 3 prose calls it "the reference drone" / "the reference body". The title page (author's uncommitted edit) now reads "Hebbian Co-Design of Fixed Wing Drones"; prose so far says "winged drone" and "fixed-wing drone" interchangeably, keep "winged drone" as the neutral phrase outside chapter 4.
- **Author-review protocol.** Questions from the author ("don't you think…?", "what do you think about…?") are answered, not acted on; edits wait for an explicit go-ahead. Structural changes are proposed as a brief first.

## 3. Where each kind of content lives

| Content | Chapter | Rule |
|---|---|---|
| General theory (RL, PPO, LSTM, EAs, CMA-ES, NSGA-II, Hebbian rules, ABCD, EPANNs) | 2, sections 2.1 to 2.4 | Theory only. Never "in this work we…". Light forward pointers allowed. |
| Prior work and the gap | 2.5 | Papers with the design choice that differs from this thesis; positioning kept to one short paragraph (2.5.3). |
| The three challenges | 3 | Challenges and the requirements they impose. **No solutions, no method names** (no "frozen", "Hebbian rules", "output layer", "CMA-ES", "NSGA-II"). **No platform details**: the drone is "a winged drone", its body "a vector of morphological parameters", its controller "a neural network". |
| Drone, genome, forest, objectives, simulator, quantities | 4 | Everything chapter 3 declined to describe (see §5). |
| Method and its justifications | 5 | Includes the two justifications removed from 2.5.3: last-layer plasticity ↔ Raghu et al. 2020 (ANIL) + Finn et al. 2017; "plasticity only pays when something varies between lifetimes" ↔ Soltoggio et al. 2018, with the body as the varying quantity. |
| Results | 6 | Must deliver what chapter 3 sets up (see §5). Figures already copied to `images/06_Results_and_Experiments/`. |

## 4. Chapter status

| File | Status |
|---|---|
| `01_Introduction` | empty |
| `02_Technical_Background` | complete (2.1 RL, 2.2 LSTM, 2.3 EAs, 2.4 Hebbian, 2.5 Related Work) |
| `03_Challenges` | complete (three sections + intro) |
| `04_Simulation_Environment` | **4.1 Genesis: The Simulator complete** (2026-09-18, 5 subsections, 3 tikz figures); **4.2 Flight Environment complete** (2026-09-18, 5 figures generated from the real `ForestGenerator`); **4.3 Drone Model complete** (2026-09-18: Shape and Actuators, Sensors (new), Genome, Standard Drone). Chapter 4 is complete |
| `05_Method` | **complete** (2026-09-21): 5.1 Base Controllers (five subsections), 5.2 The Hebbian Controller (four), **5.3 The Co-Design Pipeline** (Evaluation Flights, Inner Loop, Outer Loop, The Course of a Run, The Control Condition). 5 tikz figures, 7 tables. The author's three headings 5.3 Inner Loop / 5.4 Outer Loop / 5.5 Full Pipeline were merged into one section on his suggestion. Reviewed on the author's request the same evening: `reviews/2026-09-21_review_5.md`, nothing applied yet. The author's title still reads "DA RILEGGERE Methodology" (his own reminder: leave it) |
| `06_Results_and_Experiments` | headings only |
| `07_Conclusions_and_Future_Work` | headings only (Meta-learning, real drones, other systems) |
| Abstract, acknowledgements | empty |

## 5. Decisions and promises per chapter

### Chapter 2, section 2.5 Related Work (written 2026-09-17)

- Order: 2.5.1 Co-Design of Morphology and Control → 2.5.2 Evolved and Learned Plasticity → 2.5.3 Positioning of This Thesis. (Co-design is the problem, plasticity the tool, then the gap.)
- 2.5.1 opens with the **bilevel co-design formula** `eq:codesign-bilevel` (outer max over body m, inner max over controller θ) and uses it to compare how each work pays for the inner level. Papers: Sims 1994, Lipson & Pollack 2000, Cheney et al. 2018 (premature convergence, morphological innovation protection), Eiben & Hart 2020, Gupta et al. 2021 (DERL: PPO from scratch per body, Baldwin effect, >1000 CPUs), Muff et al. 2026 (hexacopters: (μ+λ)-ES on 36 body parameters, PPO from scratch per body, <1000 bodies), Bergonti et al. 2024 (same lab, NSGA-II outer, trajectory optimization inner, no learned controller), Gupta et al. 2022 MetaMorph (morphology-conditioned universal policy).
- 2.5.2 opens with Floreano & Urzelai 2000 and the **"Born to Learn" lesson** (plasticity is only selected when something varies between or within lifetimes). Miconi et al. 2018 gets the second formula `eq:diffplast` (fixed weight + α·Hebbian trace, all trained by gradient), Backpropamine 2019 one clause. Najarro & Risi 2020 (ABCD + η per synapse, ES, random weights every episode, static net wins on the intact body), Pedersen & Risi 2021, Ferigo et al. 2023, van Diggelen et al. 2025 (one shared ABCD set, random init per robot, CMA-ES, MARL baselines beaten).
- 2.5.3 = the gap paragraph (enumerate of the three ways co-design works pay for the inner level; the plasticity works all start from random weights or co-train by gradient) + **one** paragraph naming the thesis at a high level, mapping its plastic part onto `eq:diffplast`. The longer version with method details and the Raghu/Soltoggio justifications was **discarded on the author's decision**; those justifications go to chapter 5.
- Notes the author asked for are in place: Gupta = RL-trained controller per body; Muff = EA owns the morphology; Bergonti = trajectory optimization, no controller; Miconi = differentiable not evolved; van Diggelen = converges from random init, same rule set for the whole swarm.

### Chapter 3, Challenges (written 2026-09-17; renamed from "Problems" 2026-09-21, folder `03_Challenges`, labels `ch:challenges`, `sec:challenge-*`)

- Opening: setting in one paragraph (winged drone, forest, distance and energy), then the **three sections as three steps of one argument** ending in the requirement "a controller that adapts to the body it is given, at a cost small enough to sit inside the body search".
- 3.1 Sequential design: fix m, solve θ*(m) only; the body bounds energy per metre (airframe) and manoeuvrability (control authority); ends "a body that was never tried cannot be found". **Left out on purpose:** the reference drone + specialist controller as the sequential-design baseline. There is a `% Possible addition` comment in the source; the author may draw it from the standard-mydrone results later.
- 3.2 Morphology optimization: fix θ_G, search m; **`eq:ranking-flip`** (two bodies ranked one way under the shared controller, the other under their own); the generalist undervalues bodies at the edges of its training distribution, which is where better bodies are; training a specialist after the search cannot recover a discarded body; ends with the requirement "a score that reflects what each body can do with a controller adapted to it".
- 3.3 Feasibility: **`tab:feasibility`** (measured + hypothetical costs, see §6); noise multiplier (60 forests per body); the works that train per body stay <1000 bodies or need >1000 CPUs; ends with the shape any answer must have (controller paid once; adaptation to the scored body from elsewhere, at rollout cost) and the empirical question.
- No figure in chapter 3 (equation + table carry it). No cross-chapter references. No method names. No platform details.

### Chapter 4, section 4.1 Genesis: The Simulator (written 2026-09-18)

- Subsection order (author's three headings kept, two added): 4.1.1 **Architecture and Taichi Kernels** (new, placed first because the pros and cons need its vocabulary) → 4.1.2 Advantages and Disadvantages → 4.1.3 Physics Simulation → 4.1.4 Aerodynamics Simulation (run-in `\paragraph` heads: elements and local flow, coefficients, forces and points of application, interaction, propeller, coupling and safeguards, limits of the model) → 4.1.5 **Simulation Noise** (new).
- **No code** anywhere (author's rule for this section): formulas and three tikz figures only (`fig:taichi-kernel` life of a kernel, `fig:sim-step` one control step, `fig:aero-coefficients` C_L/C_D of the reference wing, NACA 3416, AR 3.5, curve data computed from the solver's formulas).
- Author's instructions: stochasticity is described **without CRN** (never mention common random numbers in chapter 4); the aerodynamic noise is presented as a robustness measure (domain randomization, Tobin 2017 / Peng 2018).
- Everything was fact-checked against the vendored `genesis/` (0.3.7 + lab aero solver) and the production configs. Facts that differ from what one would assume: state exchange with PyTorch is a **GPU-to-GPU copy, not zero-copy**; Taichi's Python frontend is from the DiffTaichi paper (both Taichi papers are cited); 0.3.7 runs in `ti.field` mode, so compilation is scene-specific; the integrator is the default `approximate_implicitfast`; constraint solver CG; `enable_collision=False` (trees are tensors because geometry is shared across envs); aero forces are explicit, evaluated at 100 Hz on the start-of-substep state; production aero config is the **inline** `SimpleDroneAeroParameters` (force cap 30 N, fuselage C_D0 0.35), not `aero_parameters.yaml`; wing section parameters come from `src/naca_generation/naca4.csv` (XFOIL), tail = NACA 0216 row.
- **Attribution of the aerodynamic solver (author's instruction, 2026-09-18).** 4.1.4 opens with a credit paragraph: the solver was designed and implemented by **Andrea Vicari** (PhD candidate, also on the title page) as part of his still unpublished doctoral research; it is not a contribution of this thesis, the credit is entirely his, the author thanks him, and 4.1.4 documents the model "as it was used" without being the reference description of his work. No citation exists yet: add one if his work is published before submission. The section intro points to this credit, and the rest of 4.1 says "the solver used in this thesis", never "of this thesis". Keep this wording in chapters 5 to 7 and thank him again in the acknowledgements.
- Deliberately neutral wording, because the code and the textbook differ (see §8): the force rotation is given literally as `F = R_z(β) R_y(α) (D, 0, L)^T`, the slipstream as "corrects the axial component of the flow seen by the elevator through a gain k_s" (no sign), and the per-episode randomization of the aerodynamic *parameters* is **not mentioned** (it never runs). The missing ω × r term is stated openly in "Limits of the model".
- One cross-chapter pointer: `\autoref{tab:feasibility}` in 4.1.2. Two intra-chapter pointers: `sec:flight-environment`, `sec:drone-model` (labels added to the empty sections).
- 4.2 must describe the tensor forests, the depth rays and the geometric collision test (4.1.3 defers them there); 4.3 must describe the six joints, the NACA genes and the airfoil table (4.1.4 defers them there); sensor noise, actuation latency and servo offsets are deferred to "the definition of the task".

### Chapter 4, section 4.2 Flight Environment (written 2026-09-18)

- Intro of 4.2 (before the three subsections): corridor (W = 100 m, start 30 m before the first tree, 15 m altitude, r = 0.75 m, trees infinitely tall so avoidance is planar), forests as F × T tensors, then run-in `\paragraph` heads **Perception** (20 sectors over an 80° fan, 30 m range, nearest trunk surface or lateral wall, fan narrows with cos(roll); it is a *sector* sensor, not a ray caster: a trunk blocks every sector its angular extent overlaps), **Collision** (`eq:forest-collision`, rectangle r × (b/2 cos φ + r), margin 1 cm in evaluation and 20 cm in training), **End of a flight** (defines *progress* = x at the end of the flight; the second objective is only named "energy spent per metre", to be defined later).
- Author's roles for the three generators, used verbatim as the storyline: **uniform** = internal tests, **growing** = every result of the thesis, **Latin hypercube** = explored to remove overlapping trunks and tree walls, then discarded to keep the forests natural.
- **Author's rule (2026-09-18): the difference between inner-loop forests and exam forests is NOT mentioned in chapter 4**; it belongs to the methodology chapter. 4.2 says only "The lengths and densities used in the experiments are given with the method", and mentions the per-forest randomization of λ_0 as "an option used during training" without values. Chapter 5 must therefore give: training course (150 m, λ 0…3 → 4), search course (100 m, 0 → 5), exam course (300 m, 1 → 5.5), forests per evaluation, the 80 m gate.
- Insight formulas kept: linear profile `eq:forest-density`, inverse-CDF sampling `eq:forest-inverse-cdf`, survival of a blind straight flight `eq:forest-survival` / `eq:forest-survival-growing` (exponential, memoryless in a uniform forest; quadratic exponent in a growing one), nearest-neighbour probability 1 − exp(−nπd²) inline.
- "Latin hypercube" is the author's heading and the project's name (`forest_mode: latin`), but the generator is a **jittered / fully stratified sampling (one tree per cell)**, not LHS proper; the text says so explicitly ("The name is used loosely") and cites McKay et al. 1979. The `lattice` mode gets one sentence as an earlier, more regular variant.
- Figures (all top views, trunks to scale, same 300 m × 100 m corridor and about the same number of trees so they compare directly): `forest_sensing`, `forest_uniform` (975 trees), `forest_growing` (λ 1 → 5.5, 975 trees, with a trees-per-10-m strip against the profile), `forest_random_vs_stratified` (one tree per 25 m², overlaps and closed gaps highlighted), `forest_latin` (cells 8 → 3.4 m, 988 trees). Script: `images/04_Simulation_Environment/make_forest_figures.py` (loads `src/winged_drone_train/perception/forest.py` by path, torch on CPU, fixed seeds; rerun it to regenerate). Two-colour palette (#0f8a5f trees, #D55E00 accent) checked for colour-blind separation; highlighted items also differ by shape/outline.

### Chapter 4, section 4.3 Drone Model (written 2026-09-18)

- Subsections: 4.3.1 Drone Shape and Actuators (run-in heads: servomotors and gear ratios, masses, energy), **4.3.2 Sensors (new, author's request: the depth-sensor paragraph and `fig:forest-sensing` were moved here from 4.2 "since it's more related to the drone")**, 4.3.3 Drone Genome (`tab:drone-genome`), 4.3.4 The Standard Drone. 4.2 keeps Collision and End of a flight and points to `subsec:drone-sensors`.
- **Cost of transport is defined here** (`eq:drone-cot`, CoT = E/(m g Δx), nominal mass, Δx = progress) together with the propeller power law `eq:drone-prop-power` and a qualitative servo power model (mechanical power + loss ∝ torque², never negative; no motor/ESC losses). How CoT is averaged over flights (ratio of means per body) is **left to chapter 5**.
- Sensors table `tab:drone-sensors`: 28 measurements (altitude, quaternion, world-frame velocity, 20 depth sectors) with their noise in physical units (0.1 m, 0.01, 0.4 / 0.2 m/s, 1.5 m) + 8 non-measured inputs (last 7 commands, commanded speed) = 36. Stated as absent: position along/across the corridor, angular rates, airspeed, joint angles, any information on the body. The critic's privileged observations are NOT in chapter 4 (chapter 5).
- **`wing_span` gene = span of ONE panel** (tip to tip = 2 b_p + 0.1 m; aerodynamic aspect ratio = (2 b_p + 0.1)/chord = 7.5 for the reference drone, NOT 3.5). This corrected `fig:aero-coefficients` in 4.1.4 (now AR 7.5, α_s 12.0°, C_Lmax 0.98, (L/D)max 18.7) and every wingspan-dependent number in 4.2 (b = 1.4 m in the collision test: blind flight 11 m / 26 m, closed gaps 64 % → 39 %). **The b of 4.2 is the width used by the simulator's collision test, the combined span of the two panels (2 b_p, 1.4 m), not the tip-to-tip wingspan (1.5 m)**; the text says so and states that the test ignores the fuselage, the body length and the sweep. Switching 4.2 to 1.5 m was tried and reverted on 2026-09-21 (see `CH4_CORRECTIONS.md` B3).
- The sweep/twist "multipliers" are presented as **gear ratios** (range 0.7/k and 0.5/k rad, torque 0.75 k and 0.6 k N m, damping ∝ k², friction ∝ k); tail joints ±0.25 rad, 1 N m, no gear.
- Standard drone vs Bixler 3 (author's instruction): "the dimensions and the section of its wing are those of the H-King Bixler 3", cited to the product page (`hobbyking2026bixler3`: 1.55 m span, 0.95 m length, 0.89 kg) and to Bergonti et al. (who use it as baseline), followed by the explicit caveat that actuators differ (ailerons/elevator/rudder on fixed surfaces, pusher propeller vs morphing panels, all-moving tail, tractor propeller), it is lighter (0.605 kg), and flight dynamics are **not directly comparable**. The correspondence of dimensions and wing section is stated on the author's authority (author's decision 2026-09-18: no further check needed).
- Figures: `fig:drone-actuators` = tikz schematic redrawn from slide 10 of `presentation/Final_presentation_hebbian.pptx` (vector, no spell-check squiggles) + `standard_drone_actuation.png`, a rear three-quarter render (camera behind and to the left, az 148°, el 36°, so the nose points up-left like the "nose up" of panel a) of the actuated pose (left panel swept back to its limit and twisted, right panel twisted the other way with NO sweep, at the author's request, so that sweep and twist can be told apart; elevator and rudder deflected; the caption says the same), with the moving surfaces in their neutral position as a pale translucent ghost with a grey outline, and a faint planform shadow. Third version, 2026-09-21: Genesis only poses the drone and dumps the world-frame visual meshes, Blender Cycles renders them (smooth shading, studio lights, 3 passes), PIL composes; `images/04_Simulation_Environment/make_drone_figures.sh` (needs `BLENDER=` path to a Blender ≥ 4.2, the portable archive is enough; `FIGS=actuation` rebuilds this one only; steps `drone_dump_geometry.py`, `drone_render_blender.py`, `drone_compose.py`). The author rejected as ugly both earlier versions: a flat top view, then a front three-quarter Genesis rasterizer render with a flat grey silhouette as ghost (dominated by the black propeller disc, tail looking detached, no shading). The only geometry not taken from the URDF is the propeller disc, rebuilt with a round rim (the URDF one is a 32-gon). `fig:drone-standard` redone with the same pipeline on the author's request (2026-09-21): (a) the neutral pose from the SAME rear three-quarter camera as the actuation figure, so it is its neutral counterpart (front cameras were tried and dropped: the tail looks detached, because the elevator really sits 3 cm behind and 2 cm above the tip of the fuselage), (b) top view with the nose up and a soft drop shadow. The Genesis script `render_standard_drone.py` was deleted (it would overwrite the new images; it is in git history up to commit 697debf). The champion renders of chapter 6 (`WP2_Outer_Loop.render_champions`, Genesis rasterizer, front 3/4 + top) therefore no longer share the look of `fig:drone-standard`; they can be redone with this pipeline from the champion genomes, not done.

### Chapter 5, opening and section 5.1 Base Controllers (written 2026-09-21)

- **Author's brief:** structure of the controller, the numbers of the training environment, the fitness function, how many bodies train the generalist, and a dive on the reward with every component explained; same style as the other chapters; subsections could be renamed and renumbered; **no code unless strictly necessary** (none was used: formulas, one tikz figure, three tables).
- The author's three headings (Specialist LSTM Controller, Generalist LSTM Controller, Training the Generalist LSTM Controller) became five, because the two controllers share everything except the pool of bodies: 5.1.1 **Architecture of the Controller** → 5.1.2 **Training Environment** → 5.1.3 **Reward Function** (run-in `\paragraph` heads, one per term) → 5.1.4 **Training the Generalist** → 5.1.5 **Specialist Controllers** (defined as "the same procedure with K = 1").
- Chapter opening = one paragraph: the requirement that closes chapter 3, then a roadmap of the five sections in one clause each. Labels were added to the still empty sections so that the roadmap can point to them.
- "The fitness function" of the brief is answered in two places: `eq:generalist-objective` (what PPO maximizes: the average over the pool of the expected discounted return, which restates the compromise of 3.2 as an objective; the specialist is K = 1) and the closing paragraph of 5.1.3 (CORRECTED 2026-09-21, see the 5.2 entry: the summed reward is also the fitness of the inner CMA-ES; only the search over bodies judges by progress and CoT, of which the reward is a dense, approximate proxy, so a base controller is "a competent pilot and not an optimum of either objective").
- **The reward weights in `tab:reward-terms` are the effective per-step ones, not the YAML ones.** The env multiplies every scale by `dt * 50 = 2` (`env.py::_accumulate_rewards`), and `_reward_crash` further divides by `dt * 100 = 4`, so YAML progress 0.5 → +1, smooth −0.1 → −0.2, angular −0.005 → −0.01, obstacle −0.1 → −0.2, energy −0.001 → −0.002 per W, height −0.003 → −0.006 per m², cosmetic −1 → −2 per rad², crash −10 → **−5 per crash** (confirmed by `rew_crash = −5.0` in `training_log.csv`). `success` and `stability` have weight 0 and get one sentence.
- Names used in the thesis for the code's terms: progress → **speed tracking** (it is a Gaussian on v_x / v* with width 0.25, not a distance reward; the text explains that for a given command its sum is proportional to the progress), cosmetic → **symmetry**, height → altitude, smooth → smoothness, angular → angular rate. The smoothness term acts on the *applied* commands (throttle in [0, 1], joint targets in rad, after latency and offsets); the symmetry term on the actual joint angles (sweep axes are both +z and twist axes both +y in the URDF, hence sum for the sweep and difference for the twist).
- The actor never receives the genome: stated, contrasted with MetaMorph, and used to motivate both the LSTM-first layout (implicit identification, `peng2018sim`) and the hand-over to 5.2. The critic's privileged input (67 = 36 clean + 3 + 6 + 6 + 1 + 15 genes, genome noise 10 % per flight and 2 % per step, airfoil genes excepted) is described here, as chapter 4 deferred it. The reading "so that it reads as a description of a body rather than as the label of one of the training bodies" is the writer's, not taken from a note of the author: check it.
- The output layer is written as `eq:actor-output` with **W ∈ R^{7×32} and x_t ∈ R^32**, the notation of `eq:hebb-matrix` / `eq:abcd`, so that 5.2 can apply the rules to it directly; the sentence on the 224 weights is the only forward pointer to 5.2.
- **Pointers (author's command "fix the pointers", 2026-09-21, items B5 and C1 of `reviews/2026-09-21_review_3.md`).** The unkept pointer of 4.1.5 ("are described with it") now points to `subsec:training-environment`, which is where latency, joint-target offsets and the randomized release are given: it is the second cross-chapter pointer of chapter 4, with `tab:feasibility`. In 5.1 the cross-chapter pointers that carried no definition were removed (`tab:drone-genome` in the intro, the second `subsec:actor-critic`, `subsec:genesis-noise` and `subsec:drone-sensors` in the opening of 5.1.2 since the caption of `tab:training-environment` has them, `subsec:drone-genome`, `sec:drone-model`, `subsec:genesis-architecture` in 5.1.4). Those that remain each stand for a definition that 5.1 would otherwise repeat (`tab:drone-sensors`, `eq:drone-actions`, `eq:drone-gear`, `eq:drone-cot`, `eq:drone-prop-power`, `tab:feasibility`, the chapter 3 sections): keep this criterion in 5.2 to 5.5. No review was run after this change, at the author's request.
- **Author's decisions of 2026-09-21 on `reviews/2026-09-21_review_3.md` (A1, A3, A4, B1 to B5 applied; A2 not applied).** (A3) **The controller does not know the genome, and the role of the LSTM is to make the controller stateful**: 5.1.1 motivates the recurrent layer by partial observability only (rates, airspeed, joint angles, noise, latency, servo delay) and must NOT say that the LSTM identifies the body or adapts to it; the generalist is "one set of weights for all bodies ... adapted to none of them in particular", in agreement with ch3:62, and the adaptation is handed to 5.2. Keep this line in 5.2 to 5.5 and in chapter 6. (A4) **The Hebbian rules are also evolved on top of the specialist, on the standard drone** (heading 6.1.2 "Improving Performance on Specialist Controllers"); results not available yet as of 2026-09-21, so 5.1 states the use and promises no outcome. (B4) The training range of the commanded speed (5 to 25 m/s) is stated to exceed what most bodies can hold (stall of the reference drone about 6 m/s, zero thrust at 23 m/s for every body); the text claims no intent and says nothing of the envelope at the evaluation speeds. (A2) The author considers ch3:29 all right as written; the writer's answer is recorded in the report, no edit.
- **Wall-clock times (A1, applied 2026-09-21):** `tab:feasibility` now carries the measured values of the production controllers (generalist 14 h, specialist 1.7 h on one GPU, hypothetical rows 44 800 h and 2 720 h, all in wall-clock hours of the two-GPU node), ch3:58 was recomputed (more than five years, almost four months, a factor of almost thirty), and 5.1.4 / 5.1.5 restate the two times (fourteen hours, with the cause: the 256 scenes are stepped one after the other inside each process; less than two hours).
- Deliberately left out: the fraction of training flights that end by attitude limit (93 % at the end of training in `training_log.csv`, mean final x ≈ 14 m over the random pool), which is a result, not method; the fact that mini-batches are not shuffled (only its consequence is stated: advantages standardized within a group of eight bodies); deterministic vs stochastic actions at evaluation (belongs to 5.3/5.4).
- 5.2 must pick up: the frozen actor, `eq:actor-output`, the 224 weights → 896 ABCD coefficients, the ANIL/Finn and Born-to-Learn justifications. 5.3/5.4 must give the evaluation courses, the grid of commanded speeds (`evaluation.vmin/vmax`, one speed per forest slot), the 1 cm margin and the 100° roll limit of evaluation, and how CoT is averaged.

### Chapter 5, section 5.2 The Hebbian Controller (written 2026-09-21)

- **Author's brief:** say that the way the Hebbian parameters are exploited is the main novelty; their plastic role (use the high-level latent representation of the last layer to change the flight dynamics at run time and adapt to the morphology); the hyperparameters and their meaning; how the decay avoids the runaway of the weights; **never mention the Oja rule, which was never used in the thesis** (`use_oja_coefficient: false` in every production config; chapter 2 does not mention it either); subsections could be renamed.
- The author's two headings became four: 5.2.1 **Hebbian Rules on the Output Layer** (kept) → 5.2.2 **Why the Output Layer** → 5.2.3 **Weight Decay and the Lifetime of the Weights** (was "Weight Decay and Reset Semantics") → 5.2.4 **Parameters of the Plastic Layer**. No code; two tikz figures (`fig:hebbian-controller`, the control loop through the body and the plasticity loop around the output layer; `fig:hebbian-decay`, response of one weight to a constant Hebbian term with and without decay, plain tikz `plot`, no pgfplots) and one table.
- **Novelty statement (section intro):** in the plasticity works of 2.5.2 the rules build a controller from random weights at every episode; here the controller exists and flies, the rules start every flight from the trained weights and move them by a bounded amount: "plasticity is not used to learn the task, but to adapt to a body a solution of the task that was learned for all bodies".
- **Notation:** `\mat{W}_0` = trained weights (= weights at the release), `\mat{W}_t` = weights at step t, `\vect{\mu}_t` plays the role of the `\vect{y}` of `eq:abcd`, `\mat{H}_t` = Hebbian term (the bracket of `eq:abcd`, `eq:hebbian-term`), update `eq:hebbian-update` = `W + ηH − λ(W − W_0)`, closed form `eq:hebbian-trace`. The postsynaptic activity is the mean, before sampling and before tanh (`frozen_actor.py::act`); the command of step t uses W_t and the rule then produces W_{t+1}.
- **The decay is towards the trained weights, not towards zero** (`hebbian.py`: `W ← (1−λ)W + λW_0 + ΔW`): the text says why the `−λW` of 2.4.2 cannot be used as it is. The closed form carries three stated properties: bound `η/λ × max|H|` (the ratio η/λ = 0.1 is called "the authority of the rules"), memory 1/λ = 20 steps = 0.8 s ("closer to a continuous retuning of the gains ... than to a slow learning that accumulates over the flight": the writer's reading, an honest consequence of λ = 0.05, check that the author is comfortable with it), independence from the length of the flight. **The decay does not make runaway impossible**: linear argument on one row, loop gain `g_i = Σ_j (A_ij x_j + C_ij) x_j`, growth needs `η g_i > λ`, i.e. g_i > 10; with the measured ‖x‖² (about 5, seldom above 12) this needs large coefficients of concordant sign along a whole row. The clip at ±5 000 (`w_max`) is presented as a numerical guard.
- **D is not plastic:** after about a second its contribution is the constant ηD/λ, the same on every body, i.e. a static retuning of the output layer; A, B and C depend on the activity and are the ones that can tell bodies apart. Stated in 5.2.3.
- The two justifications that 2.5.3 deferred are here: last-layer adaptation ↔ `finn2017maml` + `raghu2020rapid` (bodies read as tasks; difference: no loss in flight, a local evolved rule instead of gradient steps), and Born to Learn (`soltoggio2018born`, `najarro2020meta`) with the body as what varies between lifetimes ("fixed within a flight, hidden from the controller and different from one flight to the next"). The rules evolved on the specialist are named as "the opposite case", no outcome promised. `vandiggelen2025emergent` is cited for "identical rules, different histories, different weights".
- The pitch/elevator example of 5.2.2 is an illustration of what a correlation coefficient *can* do, explicitly not a claim about the evolved rules ("a question for chapter 6").
- Line kept from the decisions on 5.1: the rules are told the body no more than the network is; nothing says that the LSTM identifies the body.
- **All-zero coefficients = the frozen controller** (origin of the search space): used to justify the start of the search (5.3) and the control condition of the experiments (the morphology-only runs are the same pipeline with `eta: 0.0`).
- Reset semantics: W_0 and a zero LSTM state at every release (`IsolatedPopulationActor.reset_episode`), every simulated flight has its own W_t, one flight per slot per evaluation; a lifetime is one flight.
- **Correction made to 5.1.3 while writing 5.2 (factual error of the writer, 2026-09-21):** the closing paragraph said "The reward is used in this section and nowhere else. The evolutionary searches ... never see it". Wrong: **the fitness of the inner CMA-ES is the WP1 reward summed over the flight**, averaged over bodies and forests (`evaluate.py`, `reward_sums`, `cmaes.fitness_aggregator: mean`); only the outer NSGA-II judges by progress and CoT. The paragraph now says that the reward has a second use as the fitness of the rules (5.3) and that the search over bodies never sees it. 5.3 must define that fitness.
- Deliberately left out: the Oja coefficient; `rules_per_neuron`, `evolve_eta`, `evolve_decay` as options (only "could have been evolved for each weight, at the price of 448 more coefficients"); stochastic vs deterministic actions in evaluation (5.3/5.4); any result on what the evolved rules do (chapter 6); **the finding that only 12 of the 32 units of the last hidden layer are in use, see §8**.
- **Review of 5.2: `reviews/2026-09-21_review_4.md` (nothing applied).** Decisions waiting for the author: A1 (D is a static retuning, so "with against without plasticity" does not isolate plasticity: how chapter 6 attributes its gains), A2 ("adapt to the body" against a memory of 0.8 s, and a reason for λ = 0.05), A3 (the purpose of withholding the genome is nowhere in the text; ch5:291 "over any fixed weights"), A4 (inner/outer vocabulary before 5.3). Corrections B1 to B9, of which B1 (the novelty sentence misdescribes Miconi), B2 (leftover "what the actor has to find out" at ch5:79), B3 (symbol H: trace in ch. 2, instantaneous term in ch. 5) and B4 ("bodies and controllers are judged" at ch5:115) are plain `[fact]` items.
- **Author's decisions of 2026-09-21 on `review_4`:** B1 applied on the author's consideration that Miconi et al. is not a controller (ch5:220 "In the evolved plastic controllers reviewed in ...": both words keep Miconi et al., gradient-trained and described on memorization and classification, outside the statement); B2 applied ("what the actor is never told"); B3 declined (see §9); B4 applied with the author's wording: **bodies are piloted by the Hebbian controllers and judged by progress and CoT** (ch5:115; ch4:301 now "bodies are judged"). Keep this formula in 5.3 to 5.5: the rules are scored by the summed reward, the bodies by the two objectives. A1 to A4 and B5 to B9 are still open.
- (Done in 5.3, 2026-09-21: genome encoding, start from zero, fitness, courses and speeds.)

### Chapter 5, section 5.3 The Co-Design Pipeline (written 2026-09-21)

- **Author's brief:** fill 5.3, 5.4 and 5.5, possibly with the inner and the outer loop as subsections of the pipeline section; "expand them with the actual pipeline and how the evolution progresses"; move the initialization from zero and the evolutionary details from 5.2 here ("more related to how the rules are optimized than to how the rules work"); use `presentation/Final_presentation_hebbian.pptx` as a reference for the pipeline. No review afterwards ("avoid the review for now").
- **Structure:** one section, `sec:pipeline`, with 5.3.1 **Evaluation Flights** → 5.3.2 **Inner Loop: Evolving the Hebbian Rules** (run-in heads: Starting from zero, One generation, Fitness) → 5.3.3 **Outer Loop: Evolving the Drone Morphology** (The exam, Objectives, Admission, Update) → 5.3.4 **The Course of a Run** → 5.3.5 **The Control Condition**. The author's two headings are kept word for word as subsection titles. The old labels `sec:inner-loop` and `sec:outer-loop` became `subsec:inner-loop` and `subsec:outer-loop` (all references updated, chapter opening rewritten).
- `fig:pipeline` redraws slides 11 to 15 of the presentation as one tikz figure (inner CMA-ES loop nested in the outer NSGA-II loop, with the numbers of the production run); `fig:pipeline-run` is a timeline of a run (phases of four generations, exam, update; one CMA-ES search across all of them). Tables `tab:evaluation-courses` (search against exam course) and `tab:pipeline-settings`.
- **Inner and outer against `eq:codesign-bilevel`** (A4 of `review_4`, still the author's to decide): the names are kept, and the section intro says once that the outer loop is the search over m, that the inner loop is NOT the inner problem of the equation (one set of rules for all bodies; what is specific to a body happens inside the flight), and that the names refer to the nesting in time.
- **Moved from 5.2:** the start from zero coefficients and the collapse of random rules (from 5.2.4 to "Starting from zero"), the two consequences of "zero coefficients = frozen controller" (from 5.2.1: start of the search → 5.3.2, control condition → 5.3.5; 5.2.1 keeps the property and points to both). 5.2 intro: the rules are "the only part of the controller" that is searched (the bodies are searched too).
- **Wording for the control condition (A1 of `review_4` still open):** "The two conditions differ in the coefficients and in nothing else that a body can feel." It does not claim that the comparison isolates plasticity (D is a static retuning).
- **Why the rules are evolved on the reward and not on the two objectives: the author's reason (2026-09-21), now in "Fitness":** it avoids engineering another fitness function, the old cumulative reward works as it is. The text says that CMA-ES needs one number, that the cumulative reward already exists, weighs speed, survival and energy and is known to produce a good pilot, and that evolving the rules on it avoids a second fitness function with weights of its own to tune. The writer's other two reasons (the measure the frozen layers were trained for; density) were removed.
- **Author's decisions of 2026-09-21 on the writer's statements:** the remark that the CMA-ES covariance stays close to the identity (learning rates about 1e-5 per generation with n = 896) was REMOVED as unnecessary: do not bring it back. The statements on flights reaching the end of a course (saturation of the search course, "the exception" on the exam course) were REMOVED: **reaching the end of the forest is not the relevant point; what matters is that later morphologies are better than earlier ones, i.e. that the evolution makes progress.** 5.3.1 and "The exam" now say that the exam course is a harder test, the same at every exam of a run, so that successive exams show whether the bodies improve. Still the writer's and unchecked: (iv) the gate's motivation is worded with a glider that lands after twenty metres with its throttle closed (the batch_0 fronts had barely flying, cheap bodies at 5 to 20 m); (v) "the two searches shape each other" paragraph of 5.3.4 describes the design, not a result.
- **CoT aggregation, as promised by 4.3.1:** per body and per set of rules, ratio of the totals over the 480 exam flights (`evaluate.py`, `per_urdf_cot`, distance clamped at 1 cm), then the mean over the eight sets of rules; progress is the mean of the displacements. The thesis still defines progress as a coordinate in 4.2 (B1 of `CH4_CORRECTIONS.md`, open); 5.3 uses the word without redefining it.
- **`tab:feasibility`, the 94 h row (A1 of `2026-09-21_review.md`): RESOLVED 2026-09-21, author's option (a).** The 94 h and the 3 200 bodies both describe the co-design run `outer_exam_4_64_64_300_extra_r1` (50 phases × 64 bodies), which is rollouts only but not "one fixed controller". The row was relabelled "One body search in which bodies are scored by rollouts alone, with no training inside the loop" (no method name, as chapter 3 requires); numbers, hypothetical rows and ch3:58 unchanged. **The author then added by hand "(Proposed Co-Design Pipeline)" to the label and set the 94 h in bold, to mark which row is the chosen solution (2026-09-21): it is his deliberate exception to the "no solutions in chapter 3" rule of §3, it names no technique, leave it.** 5.3.4 gives "about four days" for a co-design run and 5.3.5 five days and a half for the 88 generations of the morphology-only control. Not applied, waiting for the author's word: ch3:61 "each body is flown on sixty forests before it is ranked" (in the pipeline a body is ranked on 8 × 480 exam flights; proposed "on several hundred forests").
- **Review of chapter 5 complete: `reviews/2026-09-21_review_5.md` (Opus reviewers at the author's request; nothing applied).** Waiting for the author: A1 (`tab:feasibility` prices 3 200 trainings where at most 1 600 bodies are distinct: factor about 15, not 30; and ch3:61 "sixty forests"), A2 (control condition: "nothing else that a body can feel" and the measure of its budget: 43 M flights against 61 M in total, 21.4 M exam flights against 12 M), A3 (why the commands are sampled in flights called measurements), A4 (the fitness of the rules weighs bodies by flight time), A5 (number of runs, pycma and DEAP, settings of the rules on the specialist). Plain corrections of the writer's text: B1 (the exam course is NOT "denser throughout": it is sparser than the search course beyond 29 m), B2 (1 632 → 1 600 distinct bodies), B3 (two reference-drone measurements read as one), B4, B5 (say that the depth sensor is rebuilt when the course changes: the cold reader derived the August bug from the text), B6 ("electrical power"), B7.
- **Author's decisions of 2026-09-21 on `review_5`.** A1: `tab:feasibility` is now computed on DISTINCT bodies (1 600 = 64 + 48 × 32; 22 400 h; 1 360 h; ch3:59 "two and a half years", "almost two months", "a factor of almost fifteen"); ch3:61 "sixty forests" still not decided. **A5: every simulation parameter of the experiments (number of runs, seeds, libraries, the settings of the rules evolved on the specialist, and any deviation from the settings of chapter 5) is presented at the BEGINNING OF CHAPTER 6; a difference in settings is stated only where a plot shows data obtained with different settings.** B1 (exam course "three times longer", not denser throughout nor harder), B2 (1 600), B3, B4 applied; B6 with the author's wording "power drawn from the battery" (use it wherever the energy term is described). Second round the same night: B5 applied in the author's way (the example "(for instance to change the number of trees)" removed from ch4:55; nothing about the depth sensor in 5.3.1: it was a bug that was found and fixed, not method); B8 first item applied ("phase" and "exam" defined in the intro of 5.3), the other four declined (§9); A3 (why the commands are sampled in evaluation) DEFERRED by the author; Third round: A2 applied (the budget of the control is claimed larger "in computing time, and in everything that concerns the bodies", 132 h against 94 h for the paired runs `extra_r1`, while the pipeline "flies more in total" because four fifths of its flights go to the rules; "nothing else that a body can feel" replaced), B7 covered by that sentence, A4 applied with the first sentence only (the fitness is biased towards the bodies that fly for long; no mitigating sentence). Budget of the two conditions in distance flown, for A2: co-design 7.5 million km per run (5.45 search + 2.08 exam), control 6.0 (2.39 + 3.57); node hours 94 against 132.
- **Author's answers of 2026-09-21 to the findings the readers keep raising (section F of `review_5`).** (1) **Why the actor never receives the genome: "to let the controller learn how to fly any morphology without hints on what could be the possible flight dynamics"**; now one sentence at ch5:77. Use this reason wherever the question comes up (chapter 1, chapter 7, defence); ch3:63 "by construction" and ch5:291 "over any fixed weights" are still as they were. (2) On D being a static retuning: the author's position is that the frozen controllers are flown with η = 0, which switches D off as well, so the control is clean; the writer's answer (what is unresolved is the attribution of a gain INSIDE the co-design condition between D and A, B, C) was given in chat, nothing applied; a cheap analysis for chapter 6 was offered. (3) **Progress is the distance Δx covered along the corridor from the point of release** (what the code computes, `evaluate.py`: `dx = x − x0`), no longer "the coordinate x": ch4:301 rewritten, a flight that ends at the first tree has a progress of 30 m, the blind-flight means of 4.2 say "from the first tree". Every progress value of the thesis (80 m gate, fronts) is in this convention. (4) Torque: the servomotors have a fixed limit and the limit at the wing joint is that limit times the gear ratio; ch4:76 says so (1.5 N m on the reference drone). (5) Force rotation: explained again in chat (options: ask Andrea Vicari first, then one sentence in "Limits of the model"); the author will decide at another moment (§8). **Follow-up on (2), same night: the re-fly experiment (front bodies flown again under the evolved rules, the frozen generalist, the rules with D = 0 and D alone) is the first item of `POSSIBLE_EXPERIMENTS.md`, the author's list of things to explore and then add to the thesis; he may run it before finishing. It is NOT future work: a subsection that the writer had put in chapter 7 by misreading "possible works" was removed the same night.**
- Deliberately left out: common random numbers (production runs have `crn: false`; only forests and speeds are shared), UH-CMA-ES, sigma re-inflation (`sigma_reinflate: 1.0`, off), the phase-mean fallback of the objectives, multi-node evaluation, the hypervolume and its reference point (chapter 6), how fronts are accumulated over phases (chapter 6).

### What later chapters must pick up

- **Chapter 4** must define: the drone (fixed-wing glider with actuated wing joints; 7 actions: throttle, sweep left/right, twist left/right, elevator, rudder), the 15-gene genome (see §6), the forest task, the two objectives (`progress_m` maximize, `cost_of_transport` minimize), the simulator (Genesis, GPU-batched), the stochasticity (random forests, stochastic policy) and evaluation quantities (60 forests per body, 300 m exam corridor, 80 m admission gate).
- **Chapter 5** must state the specialist vs generalist controllers (chapter 3 relies on "a controller trained on that body alone" vs "a shared generalist"), that the actor never receives the morphology, the last-layer Hebbian rules with the ANIL/Finn justification, the Born-to-Learn justification with the body as the varying quantity, weight reset semantics, CMA-ES inner loop, NSGA-II outer loop with phase-end exam.
- **Chapter 6** must deliver the comparisons chapter 3 sets up: search with a fixed generalist vs search with an adapting controller on the same bodies, forests and budget (the "morphology-only" vs "co-design" run groups); evolved fronts vs the reference drone; optionally the reference drone + specialist baseline.
- **Chapter 7** future work: the specialist baseline if still missing; meta-learning (already a heading).

## 6. Numbers and facts to reuse (with provenance)

| Fact | Value | Source |
|---|---|---|
| Generalist (WP1) training (SUPERSEDED 2026-09-21: the 29 h belong to an April run with 32 768 envs × 25 steps; the thesis now uses the 14 h of the production checkpoint, see the row "Production generalist checkpoint") | 1 000 PPO iterations, 65 536 parallel envs, 15 steps/env/iteration (~10⁹ steps), 256-body pool, 2 collection GPUs, **29 h** wall-clock | `src/WP2_Outer_Loop/experiments/batch_5/exam_4_64_64_300_extra/wp1_config.yaml`; checkpoint timestamps of `logs/remote/wp1_training/wp1_256_extra_observations_critic_multi_shard_smaller_head_r0/.../2026-04-23_20-58-28_.../tb/` (model_0 21:25 → model_999 next day 02:28) |
| Specialist training on one body | **1.7 h on one GPU** (three repeats launched together: 1 h 37 to 1 h 42 min); replaces the author's "≈ 1 h" of 2026-09-17 | run folders and `model_999.pt` mtimes of `wp1_bix_single_65536_lstm_15_r0..r2`, 2026-09-21 |
| Production co-design run | NSGA-II population 64 bodies, CMA-ES population 64, 60 forests per body → 245 760 env slots/generation, 200 CMA generations, URDF refresh every 4 → 50 outer phases, exam corridor 300 m, admission gate 80 m; **94 h** wall-clock (gen_000 2026-08-26 19:40 → gen_199 2026-08-30 17:15) | `logs/remote/outer_nsga/outer_exam_4_64_64_300_extra_r1/2026-08-26_18-19-52_.../reproducibility/config.yaml` and `generations/` mtimes |
| Hypothetical per-body training (recomputed on distinct bodies, 2026-09-21) | 1 600 distinct bodies (64 + 48 × 32; 1 568 measured in `extra_r1`) × 14 h = 22 400 h (2.6 years); × 1.7 h / 2 GPUs = 1 360 h (57 days, almost two months, against 94 h: factor 14.5). The earlier 3 200 × ... = 44 800 h / 2 720 h / factor 28.9 counted every examined body as a new training and is SUPERSEDED | arithmetic in `tab:feasibility`, `results/outer_population.csv` |
| Production generalist checkpoint | `wp1_no_bix_256_65536_lstm_15_r1`, `model_999.pt` (md5 identical to `src/WP2_Outer_Loop/experiments/batch_5/exam_4_64_64_300_extra/checkpoint.pt`); config `src/WP1/experiments/batch_10/no_bix_256_65536_lstm_15.yaml`, seed 2; wall-clock **14 h** (run folder 2026-06-07 17:06 → model_999 2026-06-08 07:09), NOT the 29 h of `tab:feasibility`, which was measured on the April run `wp1_256_extra_observations_critic_multi_shard_smaller_head_r0` (32 768 envs × 25 steps, pool seed 20), see §8 | md5sum + checkpoint mtimes, 2026-09-21 |
| Specialist run | `wp1_bix_single_65536_lstm_15_r0..r2` (`src/WP1/experiments/batch_13/bix_single_65536_lstm_15.yaml`): same YAML as the generalist except `catalog.n_urdf: null`, `lss.enabled: false`, `critic_genome_obs: false`; wall-clock **1 h 42 min** (17:56 → 19:38, 2026-07-07) | YAML diff + checkpoint mtimes, 2026-09-21 |
| Generalist training layout | 256 bodies × 256 envs = 65 536; one Genesis scene per body; 8 worker processes × 32 bodies, 4 per GPU (2 V100); lock-step stepping, single policy and PPO update on cuda:0, one rollout buffer 15 × 65 536 = 983 040 transitions; 2 epochs × 32 mini-batches of 2 048 envs (whole 15-step sequences, contiguous = 8 bodies, not shuffled), advantage normalized per mini-batch; obs normalizer frozen after 1e8 samples (≈ 102 iterations); adaptive LR ×/÷ 1.5 within [1e-5, 1e-2] around KL 0.006; episodes all start in phase (`init_at_random_ep_len` is a no-op in the sharded path) | `src/general_policy/super_scene/{runner,orchestrator,worker}.py`, `env_gen.py`, rsl_rl_lib 2.3.3 (`.venv`), read 2026-09-21 |
| Body pool of the generalist | 256 genomes, i.i.d. uniform in [0, 1]^15 (`Chromosome_Drone.random_genome`), `urdf_seed` 69, no viability filter, reference drone excluded (`include_standard_mydrone: false`) | `src/general_policy/catalog.py::build_catalog` |
| Training environment | course 150 m, λ1 = 4, λ0 ~ U(0, 3) per forest (300 to 525 trees), 10 forests per env generated once; v* ~ U(5, 25) m/s per flight (never resampled in flight: `resampling_time_s` is inert); release (−30, 0, 15) + U(±15, ±40, −10…+5) m; v0x ~ U(5, 25), v0y, v0z ~ N(0, 1) clipped ±8; attitude along the velocity; time limit 100 s = 2 500 steps; latency 0 or 1 step per flight; joint target bias N(0, 0.01 rad) per flight + N(0, 0.005 rad) per step | `wp1_config.yaml` of batch_5, `env.py` (`_randomize_reset_state`, `_resample_commands`), `perception/forest.py` |
| Reward terms (code) | progress `exp(−½((v_x/v* − 1)/0.25)²)`; crash 1/(dt·100) on collision, wall/ground or attitude limit; obstacle `Σ_20 exp(−1.5 max(ρ, 0.5))`, ρ = sqrt(x² + 24 y²)/5 on the noise-free depth; energy = total power [W]; height = (z − 10)² if z < 10; smooth = ‖Δ applied commands‖²; angular = ‖ω‖; cosmetic = (q0 + q1)² + (q2 − q3)² + q5² | `src/winged_drone_train/env.py::_reward_*` |
| Actor size | 105 863 parameters (LSTM 84 992 + MLP 20 871) + 7 std parameters; policy class `ActorCriticTanh` (rsl_rl `ActorCriticRecurrent` + tanh squashing, std a plain learned vector initialised at 0.3); critic has its own LSTM(67 → 128) | instantiated in the project venv, 2026-09-21 |
| Co-design run (production, 8 runs: 4 of 2026-08-10 + 4 of batch_5) | one pycma CMA-ES over 896 genes in [0, 1] (bounds option, full covariance, population 64, x0 = 0.5, σ0 = 0.075, default weights), 200 generations, never restarted (`sigma_reinflate: 1.0`); 64 bodies, first population random (`include_standard_mydrone: false`), phase = 4 generations → 50 phases, 49 exams (the last phase is recorded from phase means and dropped from the fronts); per generation 64 rule sets × 64 bodies × 60 forests = 245 760 slots, forests regenerated every generation, same forest ids and speeds `linspace(10, 20, F)` for every rule set and body; fitness = mean summed reward over 3 840 flights; exam = top 8 by fitness of the last generation × 480 fresh forests on the exam course; objectives = mean over the 8 of per-body mean progress and ratio-of-sums CoT; gate 80 m; 32 elites by `selNSGA2` among feasible, 32 offspring by binary tournament on (rank, crowding) over the feasible pool + SBX (p 0.9, η 15) + polynomial mutation (indpb 1/15, η 20), clip to [0, 1], airfoil genes snapped at build; all 64 re-scored at the next exam; 3 136 exam rows and 1 568 distinct genomes in `extra_r1`; wall-clock 4.0 to 4.2 days | `src/WP2_Outer_Loop/nsga_cma.py`, `nsga2.py`, `src/WP2/evolve_cma.py` (run loop l. 1740-2000), `evaluate.py` (l. 1005-1200, 862-1000), run config and `results/outer_population.csv` of `outer_exam_4_64_64_300_extra_r1`, read 2026-09-21 |
| Evaluation mode (every flight of the pipeline) | release fixed at (−30, 0, 15) m, level, 15 m/s (`env.py:917`, `reset_idx`), no release randomization; speeds from the grid; time limit 60 s (`evaluate.py::_build_multi_urdf_env`); collision margin 1 cm; limits roll 100°, pitch and yaw 90° (`env.py:487-499`); latency 0 or 1 step; sensor noise, aero noise, mass/COM and joint-offset noise on, independent per flight (`crn: false`); actions sampled (`evaluation.stochastic: true`); search course 100 m, 0 → 5 (250 trees); exam course 300 m, 1 → 5.5 (975 trees) | same files + run config |
| Diagnostics of a run (not in the search) | zero-rule baseline every 4 generations on the same bodies and course (`run_baseline`, `baseline_every: 4`); held-out validation every generation: best rule set and zero-rule baseline with the reference drone on 4 096 fresh forests of the search course (`validation`); exam baseline: reference drone + zero rules on 4 096 forests of the exam course at every exam (`outer.exam_baseline`, the star of the Pareto plots) | `evolve_cma.py`, `nsga_cma.py::_run_exam_baseline` |
| Morphology-only control (batch_4 / batch_5 `morphology_only_exam(_extra)`) | same YAML except `hebbian.eta: 0.0`, CMA population 2, `refresh_urdfs_every: 1`, 88 generations; exam k = 1 controller × 3 840 forests per body; 87 exams, 5 568 exam rows, 2 721 distinct genomes in `extra_r1`; 5.5 to 5.6 days; same seeds as the paired co-design run → identical first 64 bodies (checked for r0 to r3 on `outer_population.csv`) | `experiments/batch_5/*/run.yaml` (diff), run CSVs, 2026-09-21 |
| Wall-clock and distance of the batch_5 runs (first to last generation, file times) | co-design 98.9, 93.6, 97.5, 98.6 h (r0 to r3); control 130.8, 132.5, 128.8, 130.5 h; flights per run 61.2 M (49.2 search + 12.0 exam) against 43.0 M (21.6 + 21.4); distance 7.5 million km (5.45 + 2.08) against 6.0 (2.39 + 3.57); search flights average 110 m of 130, exam flights 165 to 180 m | `generations/gen_*` mtimes, `results/cma_summary.csv`, `results/outer_population.csv`, 2026-09-21 |
| Plastic layer (production) | η = 0.005 (global, fixed), λ = 0.05 (global, fixed), ABCD ranges [−1.5, 1.5], `use_oja_coefficient: false`, `initialize_rules_to_zero: true`, `w_max: 5000`, per-weight rules (`rules_per_neuron: false`) → 896 genes; update `W ← (1−λ)W + λW_0 + η(A xy + B x + C y + D)` then clip; y = pre-tanh mean; reset to W_0 + zero LSTM state at every episode; values tried earlier: η 0.02, λ 0.02; morphology-only runs = same pipeline with `eta: 0.0` | `src/WP2/hebbian.py`, `frozen_actor.py`, `utils.py::decode_hebbian_genes`, run config of `outer_exam_4_64_64_300_extra_r1`, `experiments/batch_5/*/run.yaml`, read 2026-09-21 |
| Output layer of the production generalist | 7 × 32: mean abs W 0.049, RMS 0.10, max 0.42; bias −0.22…0.19; learned σ 0.05…0.19. **Only 12 of the 32 input units are in use** (columns 1, 2, 7, 11, 14, 15, 19, 23, 25, 26, 28, 31: column norms 0.36–0.56, mean abs W 0.13; the other 20: column norms ≤ 0.005, activity ≈ 0.003). The three specialists have 16, 12 and 16 live units | `checkpoint.pt` of batch_5 and `wp1_bix_single_65536_lstm_15_r0..r2/model_999.pt`, computed 2026-09-21 |
| Activity of the plastic layer (frozen generalist, zero rules) | x: RMS 0.40, mean abs 0.19 (0.50 on the 12 live units), range −0.97…3.6, ‖x‖² mean 5.0, 5th–95th percentile 1.8–11.4, max 20.5, Σ abs x_j mean 6.1; y (pre-tanh mean): RMS 0.54, mean abs 0.40, max 2.35; about half of the mean square of x is a constant offset | `tesis/scripts/latent_activity_stats.py` + `.json`: 1 024 flights of the standard drone, search course 100 m (0 → 5), v* 10–20 m/s, local docker, 2026-09-21 |
| Actor architecture (production) | obs → LSTM(128) → MLP [128, 32] (ELU) → Linear(32 → 7) → tanh; 224 last-layer weights → 896 ABCD genes | `wp1_config.yaml` above |
| Morphology genome (15 genes, each in [0, 1]) | wing_span 0.4–0.9 m; wing_aspect_ratio 1.5–5; fuselage_length 0.4–0.9 m; cg_x_ratio 0.30–0.60; attach_x_ratio 0.30–0.60; elevator_span 0.2–0.6 m; elevator_aspect_ratio 1.5–4; rudder_span 0.10–0.40 m; rudder_aspect_ratio 1.5–4; dihedral −4…4°; sweep_multiplier 1.5–3.5; twist_multiplier 1.5–3.5; NACA first digit 0–4, second digit 2–5, last two 10–20 (discrete) | `src/morph_evolution/chromosome_drone.py` |
| Objectives | `progress_m` (maximize), `cost_of_transport` (minimize) | `src/WP2_Outer_Loop/configs/outer_nsga_default.yaml` |
| Simulation timing and rigid solver | control 25 Hz (0.04 s), physics 100 Hz (4 substeps of 0.01 s); `approximate_implicitfast`, CG constraint solver, collisions off, joint limits on; n_q = 13, n_v = 12; servo PD k_p = 8, k_v = 2, torque limits 1.5 N m (wing) / 1.0 N m (tail) | `src/winged_drone_train/env.py` (scene construction), `genesis/options/solvers.py`, URDF `effort`, `actuators.csv` |
| Aerodynamic model constants (production) | ρ = 1.225, μ = 1.81e-5, Oswald e = 0.8, blend width m = 0.2, CP 0.25 → 0.50 chord, k_ε = 1, k_s(tail) = 1, fuselage C_D0 = 0.35, force cap 30 N per element, speed clip 40 m/s; section slope 0.11/deg; propeller T_max = 5.2 N, C_T0 = 0.093, c_1 = 0, c_2 = 2.148, K_V = 2300 rpm/V, U = 7.4 V, f_c = 0.32 Hz, κ = 0.01 m | `genesis/engine/solvers/drones/simple_drone.py` (`SimpleDroneAeroParameters` + kernels), `actuators.csv` next to the run's URDFs (`logs/remote/outer_nsga/outer_exam_4_64_64_300_extra_r1/.../urdfs_gen_080/`) |
| Reference wing in `fig:aero-coefficients` | NACA 3416 (α_0 = −3.02°, α_s2D = 9.21°, C_D0 = 0.0135), AR 3.5 → C_Lα = 3.66/rad, α_s = 15.7°, C_Lmax = 0.94, (L/D)max = 12.8 (isolated wing, f_Re = 1) | `src/naca_generation/naca4.csv` + formulas of `_compute_coeff` |
| Simulation noise (production) | force noise σ_m = σ_d = 0.05 per substep per surface, scale s = 1…4 with incidence, CP noise σ_cp = 0.01 (std = σ_cp·s·c/4, clip 0.15 c), no noise on thrust; per-flight link mass ×(1 + 0.02 ξ), link COM + N(0, 4 mm) per axis; action latency 0 to 1 control steps | `wp1_config.yaml` of batch_5 (`aero_noise_sigma0`, `property_randomization`), `base_aero_solver.py::_apply_noise` |
| Forest geometry and perception | corridor 100 m wide, tree radius 0.75 m, start (−30, 0, 15) m; depth: 20 sectors, 80° fan, 30 m range; collision margin 0.01 m (evaluation) / 0.2 m (training); attitude limits 90° (roll 100° in evaluation) | `wp1_config.yaml` of batch_5, `src/winged_drone_train/env.py` (`check_collision`, `_compute_termination_flags`), `perception/depth.py` |
| Forest statistics quoted in 4.2 | independent positions at one tree per 25 m²: 24.6 % of trunks overlap another, 64 % have a gap < b = 1.4 m to the nearest neighbour (Poisson theory 24.6 % / 65.2 %); stratified: 6.4 % / 39 % (200 forests of 400 trees each; a check over 1 500 forests on 2026-09-21 gave 64.1 % / 38.5 %); blind straight flight, w = b + 2r = 2.9 m: mean 10.6 m in the uniform example, 26.0 m in the growing example, S(100 m) = 0.63 % | scratch computation with the real generator, 2026-09-18, checked 2026-09-21 (formulas in 4.2) |
| Drone mass model (current `UrdfMaker`) | foam 20 kg/m³ (surfaces × 0.68), fuselage shell 15 mm; fixed: battery + electronics 0.200, camera 0.030, motor + propeller 0.0415, servos 0.025 (sweep) / 0.010 (others) = 0.090, hinges and fittings 0.040 → 0.40 kg fixed; standard drone **0.605 kg**; extremes 0.44 to 2.51 kg; 400 uniform random genomes: median 0.61, 5th to 95th percentile 0.50 to 1.10 kg | `src/drone_making.py`; scratch computation 2026-09-18. NB the git-tracked `genesis/assets/urdf/mydrone/[...].urdf` is still the OLD 0.747 kg body |
| Energy model | P_prop = ρ n³ d⁵ C_P0 max(0, 1 − c'_1 J − c'_2 J²), C_P0 = 0.044, c'_1 = 0, c'_2 = 1.104 (≈ 83 W static full throttle); servo P = max(0, ωτ/(kV kI) + (R kV/kI) τ²); CoT = E/(m g Δx), m nominal, g = 9.81 | `src/winged_drone_train/control/power.py`, `src/WP2/evaluate.py` |
| Bixler 3 (commercial) | wingspan 1550 mm, length 948 mm, flying weight 890 g, 8×4 pusher propeller, 4 × 9 g servos (ailerons, elevator, rudder), optional flaps | HobbyKing product page + manual, fetched 2026-09-18 |
| Literature figures used | DERL: >1 000 CPUs (1 152), 5 M interactions/body, learning time halves within 10 generations. Muff: 36 parameters, μ=λ=24, 40 generations (960 bodies), 2.5·10⁸ steps/body. Bergonti: energy −37…74 %, mission time −22…33 % vs Bixler 3; tens of thousands of trajectory optimizations per run. Najarro: intact body static 1604 vs Hebbian 1051; unseen damage Hebbian 452 vs static 68. van Diggelen: 180 weights, 720 coefficients, CMA-ES 30 × 100, sim-to-real drop −0.7 % | fetched abstracts / full texts 2026-09-17 |

## 7. Files, labels, keys

- **Bib keys added 2026-09-17:** `sims1994evolving`, `lipson2000automatic`, `cheney2018scalable`, `eiben2020evolves`, `gupta2021embodied`, `gupta2022metamorph`, `muff2026hexacopters` (EvoApplications 2026, LNCS 16524; arXiv 2505.14129 in `note`), `bergonti2024codesign` (ICRA 2024, pp. 8679–8685), `miconi2019backpropamine`, `salimans2017evolution`, `pedersen2021evolving`, `ferigo2023evolving` (Ferigo, Iacca, Medvet, Pigozzi; IEEE TCDS 15(3)), `vandiggelen2025emergent` (arXiv 2507.11566, 2025), `finn2017maml`, `raghu2020rapid`. Already present and reused: `najarro2020meta`, `floreano2000evolutionary`, `soltoggio2018born`, `soltoggio2008evolutionary`, `miconi2018differentiable`.
- **Bib keys added 2026-09-18 (all cited in 4.1; DOIs checked on Crossref):** `genesis2024` (README BibTeX of v0.3.7), `hu2019taichi`, `hu2020difftaichi`, `tao2024genesis` (blog post re-benchmarking Genesis, Dec 2024), `todorov2012mujoco`, `todorov2014convex`, `featherstone2008rigid`, `anderson2017fundamentals`, `beard2012small`, `mccormick1995aerodynamics`, `drela1989xfoil`, `tobin2017domain`, `peng2018sim`; for 4.2: `mckay1979comparison` (Latin hypercube sampling, Technometrics 21(2)); for 4.3: `hobbyking2026bixler3` (product page, accessed 2026-09-18).
- **Bib keys added 2026-09-21 (supervisor's feedback on chapter 2; DOIs checked on Crossref where one exists):** `puterman1994markov` (definition of the MDP, with `suttonbarto2018`), `bellman1957dynamic` + `howard1960dynamic` (value and policy iteration), `williams1992simple` (policy-based methods, with `sutton1999policygradient`), `goodfellow2016deep` (2.2), `rechenberg1973evolutionsstrategie` + `beyer2002evolution` (evolution strategies), `deb2001multi` + `miettinen1998nonlinear` (multi-objective optimization: placed at the first mention of the term in 2.3.2, before the definition of Pareto dominance, on the author's instruction of 2026-09-21; `deb2001multi` alone also supports "EAs are the dominant approach" at the end of that paragraph; Crossref dates Miettinen 1998, many papers cite it as 1999), `gerstner2002spiking` (2.4).
- **Bib keys added 2026-09-21 for 5.1:** `rudin2022learning` (RSL-RL, CoRL 2021, PMLR 164), `pinto2018asymmetric` (RSS 2018, DOI checked on Crossref), `haarnoja2018soft` (tanh-squashed Gaussian, ICML 2018), `kingma2015adam`, `clevert2016elu`.
- `finn2017maml` and `raghu2020rapid` are cited since 5.2 (2026-09-21); no bib entry is left uncited on purpose.
- **Labels:** chapters `ch:challenges`, `ch:environment`, `ch:method`, `ch:results`; sections `sec:ea`, `sec:hebbian`, `sec:related-work`, `sec:challenge-sequential`, `sec:challenge-morphology`, `sec:challenge-feasibility`; subsections `subsec:ppo`, `subsec:cmaes`, `subsec:nsga2`, `subsec:hebb-postulate`, `subsec:abcd`, `subsec:evolved-plasticity`, `subsec:rw-codesign`, `subsec:rw-plasticity`, `subsec:rw-positioning`; equations `eq:hebb-basic`, `eq:hebb-matrix`, `eq:abcd`, `eq:codesign-bilevel`, `eq:diffplast`, `eq:ranking-flip`; table `tab:feasibility`; figure `fig:hebb-locality`. Chapter 4 (2026-09-18): sections `sec:genesis`, `sec:flight-environment`, `sec:drone-model`; subsections `subsec:genesis-architecture`, `subsec:genesis-proscons`, `subsec:genesis-physics`, `subsec:genesis-aero`, `subsec:genesis-noise`; equations `eq:eom`, `eq:servo`, `eq:constraint-opt`, `eq:integration`, `eq:aero-angles`, `eq:helmbold`, `eq:aero-linear`, `eq:aero-plate`, `eq:aero-blend`, `eq:aero-stall`, `eq:aero-lift-drag`, `eq:aero-force`, `eq:aero-cp`, `eq:aero-downwash`, `eq:prop-lag`, `eq:prop-thrust`, `eq:prop-induced`, `eq:aero-noise`; figures `fig:taichi-kernel`, `fig:sim-step`, `fig:aero-coefficients`. Chapter 5 (2026-09-21): sections `sec:base-controllers`, `sec:hebbian-controller`, `sec:inner-loop`, `sec:outer-loop`, `sec:pipeline` (the last four on still empty sections); subsections `subsec:controller-architecture`, `subsec:training-environment`, `subsec:reward`, `subsec:generalist-training`, `subsec:specialist-training`; equations `eq:actor-output`, `eq:actor-sampling`, `eq:reward`, `eq:reward-obstacle`, `eq:generalist-objective`; tables `tab:training-environment`, `tab:reward-terms`, `tab:ppo-hyperparameters`; figure `fig:controller-architecture`. Two labels were added to chapter 2 for 5.1 to point at: `sec:lstm`, `subsec:actor-critic`. Section 5.3 (2026-09-21): section `sec:pipeline`; subsections `subsec:evaluation-flights`, `subsec:inner-loop`, `subsec:outer-loop` (the labels `sec:inner-loop` and `sec:outer-loop` no longer exist), `subsec:pipeline-run`, `subsec:pipeline-control`; figures `fig:pipeline`, `fig:pipeline-run`; tables `tab:evaluation-courses`, `tab:pipeline-settings`. Section 5.2 (2026-09-21): subsections `subsec:hebbian-rules`, `subsec:hebbian-why-output`, `subsec:hebbian-decay`, `subsec:hebbian-parameters`; equations `eq:hebbian-term`, `eq:hebbian-update`, `eq:hebbian-trace`; figures `fig:hebbian-controller`, `fig:hebbian-decay`; table `tab:hebbian-parameters`. Section 4.2: subsections `subsec:forest-uniform`, `subsec:forest-growing`, `subsec:forest-latin`; equations `eq:forest-collision`, `eq:forest-survival`, `eq:forest-density`, `eq:forest-inverse-cdf`, `eq:forest-survival-growing`; figures `fig:forest-sensing` (now placed in 4.3.2), `fig:forest-uniform`, `fig:forest-growing`, `fig:forest-overlaps`, `fig:forest-latin`.
- **Glossary entries added:** ES, EPANN; 2026-09-18: GPU, URDF, IR; 2026-09-21: GAE, KL, ELU.
- **Images.** `images/` now has one folder per chapter (`01_Introduction` … `07_Conclusions_and_Future_Work`; the old-scheme empty folders were removed). `images/06_Results_and_Experiments/` holds 212 result figures (PNG + PDF): `pareto_overlay/`, `pareto_stats/` (+ `no_bixler/`), `pca_exam_seed_paired/`, `pca_exam_seed_pairs/`, `pca_exam_seed_pairs_independent/` (+ `seed_*/`), `champion_renders/<run>/` for every exam-scored run. `MANIFEST.md` there maps each file to its source under `logs/remote/outer_nsga/`. Smoke-test run excluded.

## 8. Open items

- **RESOLVED 2026-09-21 for the two training times (the table and ch3:58 now use 14 h and 1.7 h, see §5 and §6) and, the same evening, for the 94 h row (relabelled, see the 5.3 entry of §5); only ch3:61 "sixty forests" is left of A1 of `reviews/2026-09-21_review.md`.** Original note: `tab:feasibility` vs the production generalist (found 2026-09-21 while writing 5.1). The table says "Training of one generalist controller (1 000 PPO iterations, 65 536 parallel environments): 29 h". The 29 h were measured on an April run with 32 768 envs × 25 steps; the checkpoint that every production co-design run loads (65 536 × 15, md5-verified, see §6) took **14 h** on the same two GPUs. With 14 h the hypothetical row becomes 3 200 × 14 = 44 800 h (about five years instead of "more than ten"), and the text of 3.3 ("just over a day", "more than ten years") follows. Likewise "about one hour" for a specialist is 1 h 42 min measured (3 200 × 1.7 = 5 440 h, about seven months instead of "more than four months"; the factor "more than thirty" becomes about 58). Section 5.1 avoids restating either time and points to the table. Item A1 of `reviews/2026-09-21_review_3.md`, which also lists what the cold reviewers found in 5.1 (A2 to A4, B1 to B9; nothing applied).

- **Only 12 of the 32 latent units are alive (found 2026-09-21 while writing 5.2; author to decide whether the thesis says it).** In the production generalist 20 of the 32 units of the last hidden layer have outgoing weights below 0.004 and activities of about 0.003 (see §6); the specialists have 12 to 16 live units. Consequences: the latent representation on which the rules act is effectively 12-dimensional; for a silent input the A and B terms vanish and the C and D terms move a weight that multiplies nothing, so about 560 of the 896 coefficients have almost no effect on the commands and the effective search space of the inner loop is about 336 coefficients; the ΔW heat maps of chapter 6 will show deviations on columns that do nothing. Section 5.2 is written so that nothing in it is false either way (it quotes activity statistics over all 32 units and never says that all coefficients matter). A sentence ready to insert in 5.2.2 if wanted: "In the trained generalist only 12 of the 32 features are in use: the other twenty have outgoing weights and activities a hundred times smaller, so that the representation on which the rules act is, in effect, twelve-dimensional."

- **Float placement in chapter 4 (to do once the text is final).** Several figures and tables float to pages of their own and leave white space, e.g. `fig:forest-sensing` (alone on its page in 4.3.2), `tab:drone-genome`, `fig:drone-standard`. Tune with placement specifiers (`[tb]`, `[!htb]`), by moving the float a paragraph earlier, or with `\FloatBarrier` at subsection ends; do it last, since any text edit reshuffles the floats.

- **Body-generation findings (verified 2026-09-18, author to decide; thesis text describes the design, not these).** (a) **Airfoil genes are aerodynamically inert after the first body refresh**: the env reads the NACA code only from a `[genome].urdf` file name (`env.py` regex); the outer loop writes refreshed bodies as `ind_XXX.urdf`, so they fall back to `env.naca = '3416'` of `wp1_config.yaml`. Only outer generation 0 (and the generalist's training pool) flies its own section; afterwards camber genes do nothing and thickness acts only through the wing mass. (b) **Each wing panel's mass and aero frame are on the opposite side from its root and its visual** (`drone_making.py`: inertial/aero origin at −sign·span/2, collision/visual at +sign·span/2; Genesis FK: right root y = −0.05, right visual y = −0.40, right aero frame y = +0.30). The aircraft stays symmetric, but a sweep that looks backwards in a render moves the aerodynamic centre forwards, and dihedral acts with the opposite sign. (c) The genome's gear ratios reach the URDF (limits, torque, damping) but not the power model, which uses fixed ratios [2, 2, 2.5, 2.5, 2, 2]. (d) URDF joint velocity limits (210°/s) are not read by Genesis. (e) The Bixler star's CoT is a mean of per-flight ratios, the evolved bodies' objective a ratio of means. (f) The git-tracked standard URDF is the old 0.747 kg body while the generator now gives 0.605 kg: check which one the in-run exam baseline loaded on IZAR.

- **Aerodynamic solver vs textbook (found while fact-checking 4.1, 2026-09-18; author to decide, thesis wording is neutral until then).** (1) `_rot_yz(alpha, beta)` applies `R_y(+α)` while `alpha = atan2(w_z, w_x)` is measured towards +z: drag ends up mirrored about the chord instead of parallel to the relative wind, i.e. the resultant is tilted aft by 2α and the force along the wind is `D cos 2α + L sin 2α` (checked numerically: D = 1, L = 10, α = 8° gives 3.7 instead of 1). The sibling `lisparrow.py` has the correct signs. (2) In the x-aft aero frames `v_body_tail.x -= slip * v_ind` *lowers* the airspeed at the elevator inside the propeller slipstream. (3) The local wind uses `links_state.cd_vel` (velocity at the centre of mass): no ω × r, hence no aerodynamic rate damping (this one is stated in the thesis as a limit of the model). (4) `randomize_aero_params` is never called: `env.py` guards it with `hasattr(aero_solver, "_enable_noise")`, an attribute that no solver defines, so σ_param = 0.1 in the configs is inert; the per-substep force noise and the mass/COM randomization are live. All results are internally consistent (every body flew in the same simulator); what is affected is the absolute level of drag and cost of transport.

- Verify whether the Khepera → larger-robot (Koala) transfer claim in 2.5.2 belongs to Floreano & Urzelai 2000 (Neural Networks) or to the 2001 follow-up; adjust the sentence or the citation.
- Muff et al. is cited as the 2026 proceedings version; switch to the 2025 preprint if preferred.
- DERL text says "thousands of morphologies" on purpose (sources disagree on 4 000 vs 40 000).
- Chapter 3: the reference drone + specialist baseline (possible addition, see comment in `03_Challenges.tex`).
- Chapter 3 has no figure; a small sketch of the ranking flip could be added if wanted.
- Uncommitted working-tree changes as of 2026-09-17: chapters 02, 03, 04 (label), 05 (label), 06 (label), `Bibliografia.bib`, glossary, `images/`, this file, plus the author's own title-page edit.

## 8b. Possible experiments

`POSSIBLE_EXPERIMENTS.md` (created 2026-09-21) lists the experiments and analyses that are not in the thesis yet and could be run before it is finished, each with what, why, how, cost and where it would land. It is the author's "possible works": things to explore and then add to the thesis, distinct from the Future Work of chapter 7. When a discussion or a review ends with "this could be checked", add an item there; when the author says "add X to possible works", that is the file.

## 9. Review findings declined

Findings of a whole-thesis review (§1) that the author decided not to act on. One line each: what, where, why. The review routine reads this list and does not raise them again.

- `review_5` B8, items 2 to 5 (2026-09-21), skipped by the author: ch4:53 "a single generation of the body search" (in chapter 5's terms a generation of the inner loop); ch4:301 "a quarter of a turn" against the 100° of roll allowed when flights are scored; the collision test ignores the wing at 100° of roll, which training never allowed (keep as a prepared answer); the sign convention of the sweep joints behind the symmetry term is not stated in 4.3.1.
- `review_4` B3 (2026-09-21): $H$ is the Hebbian trace in `eq:diffplast` (ch. 2) and the instantaneous Hebbian term in `eq:hebbian-term` (ch. 5). The author leaves the symbol as it is: do not rename it, and do not raise it again.
